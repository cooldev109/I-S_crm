{
  "name": "studio-ops-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Custom operations platform for an architecture / interior design studio — WhatsApp-driven CRM",
  "workspaces": [
    "shared",
    "api",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace shared && npm run build --workspace api && npm run build --workspace web",
    "dev:api": "npm run dev --workspace api",
    "dev:web": "npm run dev --workspace web",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace api && npm run typecheck --workspace web",
    "test": "npm run test --workspace api"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}

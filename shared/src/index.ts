/**
 * Shared types — imported by both /api and /web.
 * Keep this in sync with the Prisma schema. These are the wire/DTO shapes,
 * not the DB models themselves.
 */

export * from './enums.js';
export * from './dto.js';
